/**
 * The application configuration can, optionally, be read/written to a file.
 * <p>
 * The configuration file can be created by the operator or initialised by MockTCPServer. This functionality allows it to "reset to factory settings" by the operator simply deleting the configuration file and restarting MockTCPServer with an instruction to initialise it.
 *
 * @author John McDonnell
 **/
package io.cloudracer.properties;